# @kamod-ch/tracking

Event contract (`.`), browser client (`./browser`), optional Preact adapter (`./preact`), Fetch collector (`./server`). `./postgres` is not exported yet.

See the repository README, `docs/package-boundaries.md`, and `docs/adr/`.
